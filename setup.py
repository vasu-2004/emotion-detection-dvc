from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A small ML project to detect sentiments from tweets',
    author='vasu',
    license='',
)
